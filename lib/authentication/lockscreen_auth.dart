import 'package:flutter/material.dart';

class LockscreenAuth extends StatefulWidget {
  const LockscreenAuth({super.key});

  @override
  State<LockscreenAuth> createState() => _LockscreenAuthState();
}

// using local auth this is the backend
class _LockscreenAuthState extends State<LockscreenAuth> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
